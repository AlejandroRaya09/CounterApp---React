import React from "react";
import ReactDOM from "react-dom/client";
import { HelloWordApp } from './FirstApp'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <HelloWordApp/>
    </React.StrictMode>
)